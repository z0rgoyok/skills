# Publish-handoff принятой стадии

Заполнить после `RED ACCEPT`, `STAGED ACCEPT` или принятой correction-стадии. Сохранить отдельным append-only файлом до отправки исполнителю.

## Идентичность и принятый snapshot

- Handoff ID:
- Путь этого handoff-файла:
- Основной plan-файл и исходная revision до регистрации handoff:
- Пакет и принятая фаза:
- Review verdict:
- Implementation handoff ID, path и SHA-256:
- Принятый staged SHA-256:
- Принятый staged tree OID:
- Разрешённый результат: отдельные commit и push этой стадии.

## Граница коммита

- Файлы и артефакты, которые обязаны войти:
- Файлы и состояния, которые не должны войти:
- Ожидаемая семантика RED, GREEN или correction-коммита:
- Точное commit message по правилам проекта:
- Проверка совпадения нового `HEAD^{tree}` с принятым staged tree OID:

## Проверки стадии

| Проверка | Команда | Snapshot | Результат | Evidence действительно для commit |
|---|---|---|---|---|
| ... | ... | staged tree / commit range | ... | да / нет, причина |

Для RED отметить точное ожидаемое падение semantic oracle и отдельно подтвердить успешность остальных проверок. Иное падение не закрывает hook.

## Commit-hooks

Прочитать фактические hooks и wrapper-команды. Для каждого выбрать `выполнить`, `пропустить штатным --no-verify` или `штатный пропуск не поддерживается`; общий вывод без построчного сопоставления проверок недопустим. Полностью покрытый bypass-capable hook обязательно пропустить, частично покрытый — выполнить.

| Hook или wrapper | Поддерживает штатный пропуск | Выполняемые проверки/изменения | Уже закрывающий evidence | Совпадает snapshot/message/state | Решение | Команда |
|---|---|---|---|---|---|---|
| `pre-commit` | да | ... | ... | да / нет | выполнить / пропустить | `git commit ...` / `git commit --no-verify ...` |
| `prepare-commit-msg` | нет | ... | ... | ... | выполнить | штатная commit-команда |
| `commit-msg` | да | ... | ... | да / нет | выполнить / пропустить | ... |
| `post-commit` | нет | ... | ... | ... | выполнить | штатная commit-команда |

Если хотя бы одна проверка hook не покрыта на точном snapshot, выполнить hook. Mutating-hook можно пропустить только после эквивалентного преобразования и повторной проверки принятого дерева.

Перед `git commit --no-verify` подтвердить полное покрытие обоих hooks, которые он обходит: `pre-commit` и `commit-msg`. Если один из них не покрыт, получить для него точное evidence отдельно либо выполнить commit без `--no-verify`.

## Commit и локальная проверка

- Точная commit-команда:
- Полученный commit SHA:
- Полученный `HEAD^{tree}`:
- Совпадение с принятым staged tree OID:
- Состояние staged/unstaged/untracked после commit:

## Push-hooks и публикация

- Remote и URL:
- Ветка и upstream:
- Публикуемый commit range:
- Целевая remote ref:

| Hook или wrapper | Поддерживает штатный пропуск | Выполняемые проверки | Уже закрывающий evidence | Совпадает range/remote/ref | Решение | Команда |
|---|---|---|---|---|---|---|
| `pre-push` | да | ... | ... | да / нет | выполнить / пропустить | `git push ...` / `git push --no-verify ...` |

- Точная push-команда без force:
- Local SHA после push:
- Remote SHA после push:
- Совпадение local/remote SHA:
- Ответ server-side hooks:

## Обновление управляющего состояния

- Новый статус стадии:
- Запись в реестре handoff:
- Commit SHA и remote SHA, внесённые в основной план:
- Следующий разрешённый gate:
- Блокер, если публикация не подтверждена:
