interface:
  display_name: "Макс: управление и ревью"
  short_description: "Управляемый BDD-цикл с plan-памятью"
  default_prompt: "Используй $max-plan-review, чтобы вести поэтапную реализацию через план, RED, GREEN и staged review."

policy:
  allow_implicit_invocation: false
